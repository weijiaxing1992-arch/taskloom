DEVFLOW_ADDR=127.0.0.1:8080
DEVFLOW_DB=./data/community.db
DEVFLOW_WEB_DIR=./dist
DEVFLOW_WECOM_MODE=mock
# 生产环境必须替换为至少 32 字节的高熵随机值，并通过密钥管理系统注入。
DEVFLOW_SESSION_SECRET=replace-with-at-least-32-random-bytes
# 仅 HTTPS 部署设为 true；本地 HTTP 开发保持 false。
DEVFLOW_COOKIE_SECURE=false
